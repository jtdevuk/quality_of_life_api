<h1 class="text-center m-3"><%= @place.name %></h1>
<p class="m-5"><%= @place.summary.html_safe %></p>

<%= render "shared/graph", place: @place %>

<div class="text-center mt-5">
  <%= link_to "Add #{@place.name} to my shortlist", place_path(@place), method: :post, class: "btn btn-success" %>    
</div>

<%# current_user.places << @place %>